install:
	npm install
	npx prisma generate

dev:
	npm run dev

build:
	npm run build

migrate:
	npm run migrate

seed:
	npm run seed

reset-db:
	bash scripts/reset-db.sh

lint:
	npm run lint

test:
	npm run test