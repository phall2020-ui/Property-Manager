{
  "name": "@property-manager/types",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'No linting for types package'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests for types package'",
    "clean": "rm -rf node_modules"
  },
  "dependencies": {
    "zod": "^3.22.2"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
